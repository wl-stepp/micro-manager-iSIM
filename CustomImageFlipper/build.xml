<?xml version="1.0"?>
<project default="jar" name="ImageFlipper">
    <import file="../javapluginbuild.xml"/>
    <target name="copy-files" depends="mm.javabuild.copy-files">
        <copy todir="${intdir}">
            <fileset dir="${rscdir}" includes="**/*.png"/>
        </copy>
    </target>
</project>
